# Where the contact form sends submissions
CONTACT_TO=gabriel@hax429.me

# Stalwart SMTP credentials (used by /api/contact)
SMTP_HOST=mail.hax429.me
SMTP_PORT=587
SMTP_USER=no-reply@hax429.me
SMTP_PASS=replace-with-stalwart-password

# Optional: override the From: header (defaults to SMTP_USER)
# SMTP_FROM="Portfolio Contact <no-reply@hax429.me>"
